name: Release

on:
  release:
    types:
      - published

jobs:
  release:
    strategy:
      matrix:
        include:
          - runtime: 'linux-x64'
            os:      'ubuntu-latest'
          - runtime: 'win-x64'
            os:      'windows-latest'

    runs-on: ${{ matrix.os }}

    steps:
      - name: Git checkout
        uses: actions/checkout@v6

      - name: Setup dotnet
        uses: actions/setup-dotnet@v5
        with:
          dotnet-version: '10.0.x'

      - name: Build protodec
        run:  dotnet publish --configuration Release --runtime ${{ matrix.runtime }} /p:Version=${{ github.ref_name }} /p:ContinuousIntegrationBuild=true

      - name: Pack LibProtodec
        if:   matrix.os == 'ubuntu-latest'
        run:  dotnet pack --configuration Release --no-restore --no-build /p:Version=${{ github.ref_name }}

      - name: Release
        uses: softprops/action-gh-release@v2
        with:
          files: |
            bin/protodec/Release/net10.0/${{ matrix.runtime }}/publish/protodec*
            bin/LibProtodec/Release/*nupkg
            bin/LibProtodec.Reflection/Release/*nupkg
